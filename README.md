Проект для работы с финансами: пользователь ежедневно вносит актуальные суммы по каждой созданной категории. Динамику можно отслеживать через таблицу, где все валюты конвертируются в евро на стороне бэкенда. Пользователь вводит суммы в тенге, долларах, рублях или других валютах, но отображение всегда происходит в евро. Если данные за текущий день не внесены, верхняя плашка на фронтенде отображается красным цветом, а при наличии данных — зеленым.

## Запуск проекта

__Перед запуском проекта необходимо установить зависимости. 
Как это сделать для каждой из OS, описано в файле 
[INSTALL](./INSTALL.md)__.

Проект не развернут на виртуальной машине, так что пощупать его можно будет только на локальной машине.


Чтобы запустить проект в первый раз, используйте команду:
(собираются и запускаются контейнеры, создаются таблицы в БД)

```bash
make build
```

Чтобы запустить уже собранные контейнеры, используйте команду:

```bash
make run
```

Чтобы остановить и удалить контейнеры, используйте команду:

```bash
make down
```

## Миграции
Чтобы создать новую пустую миграцию с заданным именем, используйте команду:

```bash
make create-empty-migration
```

Чтобы откатить последнюю примененную миграцию, используйте команду:

```bash
make downgrade
```

Чтобы узнать, в каком статусе миграции проекта, используйте команду:

```bash
make status
```

Чтобы открыть веб-версию, нужно ввести адрес: http://localhost:7070

Так же было положено начало настройки работы проекта через веб-апп в телеграм, но не было реализовано до конца.
